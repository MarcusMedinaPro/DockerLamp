# Copy this file to .env (or run setup.sh / setup.cmd) before using
# build/run/stop/test scripts.

WEB_PORT=8080
MYSQL_DATABASE=dbdockerlamp
MYSQL_USER=User
MYSQL_ROOT_PASSWORD=changeme
MYSQL_PASSWORD=changeme
PHPMYADMIN_PORT=8081
